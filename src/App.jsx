import { useContext } from "react";
//import { AppContext } from "./Store/AppContext";

function App() {
    //const post = useContext(AppContext);

    return (
    <div>
        <h1>Noticias</h1>
    </div>
)};

export default App;